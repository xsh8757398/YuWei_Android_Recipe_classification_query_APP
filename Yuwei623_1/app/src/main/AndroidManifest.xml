<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.du.yuwei623_1">

    <application
        tools:replace="android:allowBackup,icon,theme,label"
        android:allowBackup="true"
        android:icon="@drawable/yuwei"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Register"/>
        <activity android:name=".Login">

        </activity>
        <activity android:name=".MainPage"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".Login_Message"/>
        <activity android:name=".ForgetPsd"/>
        <activity android:name=".MainFragment_2_ListMenu"/>
        <activity android:name=".OneDish"/>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>